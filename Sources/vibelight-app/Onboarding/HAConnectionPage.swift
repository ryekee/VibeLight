import SwiftUI

struct HAConnectionPage: View {
    @ObservedObject var viewModel: OnboardingViewModel
    var body: some View {
        Text("HA Connection — implemented in Task 9").foregroundColor(.secondary)
    }
}
