import SwiftUI

struct NetworkConfirmPage: View {
    @ObservedObject var viewModel: OnboardingViewModel
    var body: some View {
        Text("Network — implemented in Task 10").foregroundColor(.secondary)
    }
}
