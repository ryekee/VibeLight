import SwiftUI

struct LightSelectionPage: View {
    @ObservedObject var viewModel: OnboardingViewModel
    var body: some View {
        Text("Light Selection — implemented in Task 9").foregroundColor(.secondary)
    }
}
